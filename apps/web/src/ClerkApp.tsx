import { useEffect, useState } from 'react';
import { SignIn, useAuth, useClerk } from '@clerk/clerk-react';
import App from './App';
import { api, setToken } from './lib/api';
export default function ClerkApp(){ const {isLoaded,isSignedIn,getToken}=useAuth();const {signOut}=useClerk();const [ready,setReady]=useState(false),[error,setError]=useState(''),[attempt,setAttempt]=useState(0);useEffect(()=>{let cancelled=false;if(!isLoaded)return;if(!isSignedIn){setToken('');setReady(false);setError('');return}setReady(false);setError('');void (async()=>{try{const value=await getToken();if(!value)throw new Error('Clerk did not issue a session token');setToken(value);await api.me();if(!cancelled)setReady(true)}catch(reason){setToken('');if(!cancelled)setError(reason instanceof Error?reason.message:'Unable to authorize this workspace')}})();return()=>{cancelled=true}},[attempt,getToken,isLoaded,isSignedIn]);if(!isLoaded)return <div className="splash">Loading secure workspace…</div>;if(!isSignedIn)return <div className="clerk-login"><SignIn routing="hash" signUpUrl="#/sign-up"/></div>;if(error)return <main className="splash auth-error" role="alert"><h1>Workspace authorization failed</h1><p>{error}</p><div><button className="button primary" onClick={()=>setAttempt(value=>value+1)}>Try again</button><button className="button ghost" onClick={()=>{setToken('');void signOut()}}>Sign out</button></div></main>;if(!ready)return <div className="splash">Authorizing workspace…</div>;return <App key={api.token()}/>; }
