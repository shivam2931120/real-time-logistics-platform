import { StrictMode } from 'react';import { createRoot } from 'react-dom/client';import { ClerkProvider } from '@clerk/clerk-react';import App from './App';import ClerkApp from './ClerkApp';import './styles.css';const key=import.meta.env.VITE_CLERK_PUBLISHABLE_KEY;createRoot(document.getElementById('root')!).render(<StrictMode>{key?<ClerkProvider publishableKey={key}><ClerkApp/></ClerkProvider>:<App/>}</StrictMode>);
